# =============================================================================
# ZMienne ŚRODOWISKOWE - System Zarządzania Umowami
# =============================================================================
# Skopiuj ten plik do .env.local i dostosuj wartości
# =============================================================================

# -----------------------------------------------------------------------------
# Baza Danych
# -----------------------------------------------------------------------------
MONGODB_URI=mongodb://localhost:27017/contracts_app

# -----------------------------------------------------------------------------
# Autentykacja NextAuth.js
# -----------------------------------------------------------------------------
NEXTAUTH_SECRET=generuj-unikalny-klucz-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Azure Active Directory SSO (NOWE - wymagane dla logowania przez Microsoft)
# -----------------------------------------------------------------------------
# Pobierz te wartości z Azure Portal → Azure Active Directory → Rejestracje aplikacji
AZURE_AD_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_AD_CLIENT_SECRET=twoj-klucz-tajny-klienta-z-azure-portal
AZURE_AD_TENANT_ID=yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy
# Lub użyj 'common' dla multi-tenant:
# AZURE_AD_TENANT_ID=common

# -----------------------------------------------------------------------------
# OpenAI API (wymagane dla OCR i AI)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-twoj-klucz-openai

# -----------------------------------------------------------------------------
# SMTP - Powiadomienia Email (opcjonalne)
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.twoj-server.pl
SMTP_PORT=587
SMTP_USER=twoj-email@twoja-domena.pl
SMTP_PASSWORD=twoje-hasło-smtp
SMTP_FROM=noreply@twoja-domena.pl
SMTP_SECURE=false

# -----------------------------------------------------------------------------
# Redis - Kolejki zadań (opcjonalne)
# -----------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------------------------
# Weryfikacja Podpisów Kwalifikowanych (NOWE)
# -----------------------------------------------------------------------------
# OCSP Responder URL - do weryfikacji statusu certyfikatów
OCSP_RESPONDER_URL=https://ocsp.example.com

# Time Stamping Authority URL - do weryfikacji znaczników czasu
TSA_URL=https://tsa.example.com

# Zaufane urzędy certyfikacji (rozdzielone przecinkami)
TRUSTED_CA=Certum Trusted Root CA,GlobalSign Root CA,Microsoft Root CA

# -----------------------------------------------------------------------------
# Przechowywanie Plików
# -----------------------------------------------------------------------------
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=20

# -----------------------------------------------------------------------------
# Aplikacja
# -----------------------------------------------------------------------------
NODE_ENV=development
APP_PORT=3000

# -----------------------------------------------------------------------------
# LDAP (deprecated - użyj Azure AD SSO)
# -----------------------------------------------------------------------------
# Te zmienne są zachowane dla kompatybilności wstecznej
LDAP_URL=
LDAP_BIND_DN=
LDAP_BIND_CREDENTIALS=
LDAP_SEARCH_BASE=
